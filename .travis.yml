language: ruby
dist: trusty
rvm:
 - 2.5
install: git clone https://github.com/${TRAVIS_REPO_SLUG%%/*}/PedOSos.github.io ~/PedOSos.github.io
script: ~/PedOSos.github.io/_utils/travis.sh
addons:
  apt:
    packages:
    - hunspell
    - pandoc
    - jq
notifications:
  email:
    recipients:
      - adw@PedOS.org
    on_success: never
    on_failure: always
